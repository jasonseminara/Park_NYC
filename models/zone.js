class Zones {
  constructor({id, zone_number, price, time_limit}) {
    this.id = id;
    this.zone_number = zone_number;
    this.price= price;
    this.time_limit = time_limit;
  }
}

module.exports = Zones;
